---
source_url: https://www.youtube.com/watch?v=dypHkOKOLt8
video_id: dypHkOKOLt8
uploader: "Codacus"
title: "I Split Qwen 3.8 27B Model Across Mac and Nvidia PC (llama.cpp RPC Guide)"
captured_date: 2026-08-25
status: raw_capture
provenance: youtube_video_transcript
---

# I Split Qwen 3.8 27B Model Across Mac and Nvidia PC (llama.cpp RPC Guide)

**Video URL**: [https://www.youtube.com/watch?v=dypHkOKOLt8](https://www.youtube.com/watch?v=dypHkOKOLt8)  
**Channel / Creator**: Codacus  
**Extracted Words**: 3146 words  

## Clean Transcript

Few days back, Qwen released their new for local AI. It's trading blows with models way bigger than it, and I really wanted to use this one for work. But, my setup has 12 gigs of VRAM. That's not enough to run it at Q4, not unless I offload some of the layers back to RAM. And that gets me a laughable seven tokens a second, which is expected really, because this is a dense model, not an MoE. So, I thought, "Fine, let's just get a bigger card." But, before I spend that kind of money, I wanted to see what the machines I already have could do together. And this is that. Same model, 128K context, 22 tokens a second, running across a Mac and an Nvidia PC at the same time. Metal on one end, CUDA on the other. So, how well does this actually work? What does it cost me? And how much am I losing compared to just putting both cards in one box? Let's find out. So, my daily driver is the Qwen 35B MoE, and it runs at around 70 tokens a second. Now, the older 27B was a little more capable, but not enough to make up for running it 10 times slower. But, the new 27B changed that. See, this time there was no matching 35B MoE. So, I wasn't trading a bit of capability for a lot of speed anymore. I was locked out of the best model I could actually run by a few gigs of VRAM. And that one really hurts. Okay, so I ran it anyway with the layers offloaded. Seven tokens a second against 70. Yeah, I can't work like that. So, that left me two options: buy a 24 gig card like a 3090, or add another 3060 and build a multi-GPU box. But, before I bought anything, I wanted to try the hardware already sitting in my house. A Linux PC with CUDA, a Mac with Metal, talking over my local network. Now, that sounds like a terrible idea. But, in theory, the weights only cross once during loading. And after that, the traffic is mostly activations. And if that stays small, then maybe the network isn't the problem everyone assumes it is. Now, if you only have one machine, stay with me. The MTP trick later in the video works on a single card, too. And if you just want the result without the context, the TLDR is in the pinned comment. But, if you're here for the journey, buckle up. So, there are two ways to divide a model across GPUs. The first one is tensor parallelism. It divides every layer into shards and has all the GPUs work on that layer together. Now, that can add speed as well as memory, but it needs the devices to talk to each other on every single layer. Across one motherboard with a fast interconnect, sure, that can work. Across my home network, it would be a disaster. So, the alternative is a sequential split. One device gets the first block of layers, the next one gets the rest. And the activations cross the network at the boundary. Think of it like an assembly line rather than a bunch of people doing the same job together. Now, with two machines, that should mean one forward handoff per token. And I measured that handoff at about 92 kilobytes, under 2 megabytes a second at 20 tokens a second, which is nothing for a gigabit network. But, here's the thing. Sequential splitting does not divide the compute time. Each device waits for the previous block. So, the total is still the work of both machines added together. This buys capacity, not speed. And it's only faster here because the alternative, pulling dense model weights from system RAM for every single token, is so much worse. Okay, so the setup is two commands. So, you build llama.cpp on both machines with ggml RPC on and then on the machine you're borrowing you start GGML RPC server and give it a port. And on your main machine you run llama server the way you normally would and add one flag {dash} {dash} RPC with the other machine's address and port. That's the whole thing. Right, so I tested it with Gemma 12B which already fits on the 3060 and I attached my CPU as the second device just to watch the mechanism work. And prefill dropped from 1,248 tokens a second to 16. Yeah, 78 times slower. And nothing erred so it took me a while to work out what had actually happened. See, the default split follows the memory each device reports not its speed. And my CPU offered 61 gigs of system RAM against the GPUs 12. So it took most of the layers and computed them at CPU speed. Okay, I agree that was a stupid run. But now I know that it actually works. So let's use a real GPU on the other end and I brought in the 1060. And generation dropped from 39 tokens a second to 24. Because the 1060 is much slower than the 3060 and in a sequential split the faster card just spends that difference waiting. So both smoke tests were worse than using one device. But neither of them tested the case I actually cared about. A dense model that can't fit on the card at all. Okay, so let's do the real one. Gwen 3.8 27B the model I actually came for. Dense, 16 gigs and it doesn't fit the card. Now the 3060 and the Mac together with the layers split across both and it loads the whole model. No offloading to system RAM, just sitting in GPU memory across two machines, which is 13.7 tokens a second against 6.8 on the 3060 by itself. Except the Mac on its own does 14. So, I wired two machines together, split a model across a network, and landed on the number that one of those machines gives me for free. I mean, the 3060 might as well be switched off. Anyway, I went looking for where the speed was going. Now, my first guess was the layer order because that's the one I've been burned by before. If llama.cpp splits them badly, you can end up with your local and remote layers interleaved, a few here, a few there. And then you're crossing the wire dozens of times per token instead of once. And there's a flag for that, dash dev, where you tell it what order your machines go in. And you can check it worked by looking at how much data crosses per token. So, mine showed 92 kilobytes, which is about one forward handoff, meaning the order was already right. Still, worth knowing that number though, because if yours is up in the megabytes, your layers are interleaved and you just haven't noticed. Right, quick side note, if you like these kind of breakdowns and experiments, you know, pushing the limits of local AI, running models that we can't normally run, that's the kind of thing we do on this channel. So, subscribe if that's your thing. There's a lot more coming. Now, back to the video. Okay, so I changed the tensor split, which controls of the model each machine holds, and moving more layers onto the 3060 held until a 7 and 1/2 to 2 and 1/2 split. An 8 to 2 split that wouldn't load at all. And near that limit, performance actually started going backwards. The weights fit, but the card had no room left for the KV cache and the compute buffers. So, the packet capture is what finally showed me what I'd been missing. See, that 92 kilobytes came from the kernel's byte counter, but when I actually captured the packets, it was 431 because the counter only counts one direction. So, everything coming back was invisible to it. And the return traffic, it had a shape to it. 64 bursts, one per token, 392 kilobytes each, which is about 100,000 floats. And 100,000 is the size of the vocabulary because that that's the output head. It's the last thing the model does. It scores every token it could possibly say next, and there are 100,000 of them. So, whichever machine holds the tail of the model holds that, which means if the tail is sitting on the far side of the wire, that entire vector comes back over the network on every single token. And look, my original assumption was almost right. I'd talked myself into this by saying we're only shipping activations, so it can't be much data, and I'd just forgotten about the output head because it's true of every layer except the last one. And the fix, the fix is one line. In dash dev, I had the 3060 first and the max second. So, the Mac was holding the tail. Put the Mac first and the 3060 last instead, and the tail comes home. Now, when you flip the device order, you also have to flip the split numbers. So, mine went from 7th to 37, keeping the same layers and the same workload on each machine while moving the model's tail. And that took me from 13.69 to 16.39. So, RPC got me to 16.4, and the final jump to 22 came from MTP, multi-token prediction. Now, it works like speculative decoding except the draft layer is already inside the model file. It guesses several tokens ahead and checks them together. I made a whole video on this when it landed, so I won't do it again here. But, the important distinction is this. RPC took me from 6.8 to 16.4, and then MTP stacked on top and took 16.4 to 22. And MTP also works on one machine. So, I set the draft depth to three because that's what every benchmark lands on. And then I noticed my own server was slower than my own benchmark. The only difference was the sampling. See, benchmarks run at temperature zero, and my thinking server runs at temperature one. And at temperature one, depth three turned out to be slower than depth one. Now, the cause was acceptance. How often those predicted tokens are actually correct? Across five prompt types, Qwen 3.6 averaged around 0.84, while 3.8 averaged 0.79. And every rejected prediction is work thrown away. So, higher temperature sampling rejects more of them. So, I checked whether temperature itself was just making generation slower by turning MTP off. The model gave me 15.50 at temperature zero and 15.48 at temperature one, effectively identical. So, the loss only showed up when MTP was active, which points back to acceptance rather than sampling cost. Now, most speculative decoding benchmarks use temperature zero. So, the depth they recommend may be answering a different question from the one you're actually asking. Tune MTP at the temperature you actually use. It also creates a second context of a little over a gigabyte, so leave room for it when you're choosing the split. And the draft layer sits at the the tail, which makes the device order from the previous section even more important. So, with everything tuned, I get 22 tokens a second from an empty context and around 19.5 after the context grows. And the 6.8 baseline was measured the same way. So, the comparison stays fair. So, speed turned out not to be the main benefit here. Memory capacity was. See, the Mac alone can run this model with MTP at around 16 tokens a second, which initially made the whole cluster look completely unnecessary. And then I tried to use it with a real conversation. Now, macOS gives its GPU about 18 gigs of the 24 available. The model uses nearly 16, and MTP takes another gigabyte or so for its second context, which leaves roughly 1 gigabyte for the entire KV cache. So, the model runs, but paste in a long document or keep talking to it and memory becomes the limit almost immediately. Now, splitting the model gives each machine fewer weights and lets both of them hold KV cache for their own layers, and that gave me 128,000 tokens of context with a full F16 cache and no cache quantization. So, in some tests, the remote worker did only a small fraction of the compute. Its real contribution was freeing memory on the main machine, and a short empty context benchmark undersells that completely. So, the Mac and the PC do not need matching hardware. Each worker uses its own back end, Metal on the Mac, CUDA on the PC, and just sends numbers over the wire. But, the setup does have four traps. First, pin each worker to the device you actually want with -d. Otherwise, the Mac might advertise Blast as another compute device and crash. Second, build separately for different Linux versions or just use matching containers. Third, keep the llama.cpp build identical at both ends because a mismatch looks very misleadingly like a network failure. And fourth, on a Mac, remember that only about 75% of unified memory is available to the GPU by default, unless you raise the limit. Now, I hit that Linux version problem myself by copying a binary from Ubuntu 24 to an Ubuntu 22 machine. It looked portable, but the older C library just could not satisfy the binary. So, rebuild locally or use the same container on both machines. That avoids the entire class of failure. But most importantly, RPC server has no authentication, and it should never be exposed to the internet. Keep it on a restricted LAN or a VPN. Firewall the port to specific machines, and do not run it as root. A VPN helps, but access to that port should still be limited. The fair alternative is the box I nearly built. Another 3060 on the same motherboard using PCIe instead of Ethernet. I cannot benchmark a machine I do not own, but the packet captures let me estimate how much time the network adds. On my cleanest test, one token took 41.2 milliseconds. The two cards spent 38.5 milliseconds computing, and 2.7 milliseconds transferring data in both directions. The network accounted for about 7% of the total. That lets me build an estimate, not a benchmark. Remove the measured transfer time while holding everything else constant. For Qwen 27B, that moves 16.4 tokens a second to roughly 17.6. With MTP, the 22 token result rises to around 23 because several predicted tokens share one network pass. So, in this setup, replacing the LAN with a perfect link buys roughly one token a second, not the huge gap I expected. The slowest device was the main bottleneck, not the wire. That does not make every network equal. Latency compounds across hand-offs, and Wi-Fi was roughly half as fast as Ethernet during pre-fill, even when generation looked the same. Keep this local and wired if you can. The 7% figure also assumes a clean layer order with only the necessary crossings. My worst configuration crossed the network eight times per token and paid closer to 14%. Even there, compute on the slowest device remained the larger cost. A single multi-GPU box can also use tensor parallelism, which this sequential network layout cannot. That is a real advantage, but it requires the right model support and a sufficiently fast interconnect. So, what does all of this actually cost me? Well, the first cost is imbalance. I added the slower 1060 as a third worker, and generation dropped from 13.2 to 11.6 tokens a second because the faster machine's were just sat there waiting for it. And device order made this even worse, putting the model's tail on the weakest network link that initially produced a 33% drop. But fixing the order reduced that penalty to 12%. So, with more than two machines, put the tail on the best link rather than blindly keeping it local. Now, the second limit is the model type because this entire success case is a dense model. Okay, so a 54 gig MoE gave me 12.5 tokens a second on one tuned machine and 12.4 across two. Which, when you think about it, does make sense. I mean, only a few experts run per token, so leaving inactive experts in system RAM was already cheap. RPC had almost nothing to recover. And I saw the same thing on a smaller 26B MoE. It was 50 tokens a second with CPU spill on 1 3060 and 49 over RPC. Now, for MoE, the expert offload flags can also force a different layer order. So, the best ordering for this dense model is not a universal rule. Yeah, treat it as a per model decision. And the final limit is memory. So, I tried a 155 gig model with only 73 gigs of local memory. And both GPUs sat at 6% utilization while the weights streamed off the disk. But that machine needed more RAM, not more accelerators. So, before you tune a split, check GPU utilization. Because if it stays in single digits, the GPUs probably are not your bottleneck. So, should you actually do this? Well, it depends. It depends on the shape of your model. If it's dense and too big for your card, then yes, this is worth doing. That's the case I've been in all video, and it runs somewhere between two and four times faster than spilling into RAM. If it's dense and it already fits on one card, then don't. You'll lose about 40% for your trouble. If it's an MoE that nearly fits, it's a wash on generation. Prefill gets better, and it improves the deeper your context goes. So, it's worth it for long documents and not for much else. And if it's an MoE far too big for everything you own put together, it won't help at all because you're disk bound and your GPUs are idle regardless. So, on my setup, the dense Qwen 27B went from around seven tokens a second with RAM offloading to 22 across the Mac and PC. And against the Mac alone, the gain is a more modest 1.6 times. Both numbers are useful. They just answer different questions. Now, if you only have one card, MTP and temperature specific draft depth tuning still gave me about 1.5 times more speed without another machine. So, before buying a bigger GPU, check whether the model is dense or MoE and whether your bottleneck is actually GPU compute because that answer matters more than the number of machines you can connect. And if you've got machines sitting there doing nothing, tell me what's in them. Comment it down below and I'll see you on the next video.
