---
source_url: https://www.youtube.com/watch?v=mmbkP8NARH4
video_id: mmbkP8NARH4
uploader: "NVIDIA Developer"
title: "Get Started with Unsloth Studio: Generate Data & Fine-Tune LLMs Locally on any NVIDIA GPU"
captured_date: 2026-08-25
status: raw_capture
provenance: youtube_video_transcript
---

# Get Started with Unsloth Studio: Generate Data & Fine-Tune LLMs Locally on any NVIDIA GPU

**Video URL**: [https://www.youtube.com/watch?v=mmbkP8NARH4](https://www.youtube.com/watch?v=mmbkP8NARH4)  
**Channel / Creator**: NVIDIA Developer  
**Extracted Words**: 1456 words  

## Clean Transcript

Today, I'm going to talk about one of my favorite open source projects, Onslaught. It's a framework that makes it incredibly efficient to experiment with modern AI models on consumer grade hardware such as your GeForce RTX GPU. They recently released Onslaught Studio, which is a graphical interface that lets you generate data, train models, and much more. In this video, I'll show you a simple example where we teach a small language model to explain its answers more clearly all through Studio. Before we see how it's done, let me show you a quick demo. The model before fine-tuning is on the left side, while the model after fine-tuning is on the right side. Both models are given the same question. The model on the left side responds briefly, whereas the model on the right side takes a bit longer to respond because it's trying to explain the reasoning behind its answer. So, how did we get from that base model to the improved version? We use a simple distillation workflow. Where a larger teacher model generates improved responses from a seed dataset, and those responses are used to train the smaller model to provide the same style of answers. Let's see how it's done. To follow along, you'll need a few things. First, the latest version of Onslaught, which includes Onslaught Studio. Here's the command I used to install it. Second, a GPU with at least 16 GB of VRAM, which is sufficient for fine-tuning the smaller model. In this video, I'll be using the NeMo Tron 3 Nano as the teacher model. You can either deploy it locally or access it through a cloud provider. Here's the command I used to deploy this model locally through llama.cpp. With that in mind, let's take a look at what we're actually going to build. The first step in this workflow is synthetic data generation. We start with a seed dataset, in this case the Onslaught Alpaca Clean dataset from Hugging Face. This dataset contains instructions, optional inputs, and short answers. It's a common format used for instruction tuning. Instead of using the dataset as is, we're going to augment it using synthetic data generation. We'll use a larger teacher model to rewrite the answers so they include step-by-step reasoning along with the final answer. Once the synthetic dataset is generated, we'll use it to fine-tune a smaller model teaching it to think through the problem step-by-step and provide more explanation. The synthetic data generation workflow is powered by NVIDIA NeMo Data Designer. Let's jump into Onslaught Studio and build this workflow visually. To create our synthetic data workflow, we'll navigate to the recipes tab. From the new recipe menu, we select start empty. This opens the recipe editor where we can visually assemble the pipeline used for synthetic data generation. We'll begin by adding a model provider block. This block tells the system where the model endpoint for inference and data generation is located. Here, we specify a provider name. I'll call it my local model provider and provide the URL for that endpoint. If you're using a remote model provider such as openrouter or build.nvidia.com, you can also add an API key. Next, we add a model configuration block. This block defines the specific model that will generate our synthetic data. We'll give this configuration an alias, local NeMo Tron, and then specify the model name as exposed by the provider endpoint. Here, we can set the maximum number of tokens to 2048. If the model supports additional inference parameters, they can be provided here as well. For our NeMo Tron model, we enable thinking mode by passing the appropriate keyword arguments. Now, we'll add our seed dataset, which will be used to generate synthetic examples. We add a Hugging Face dataset block and specify the dataset repository. In this case, we use the Onslaught Alpaca Clean dataset. After clicking load, we can preview the data. For our synthetic data generation workflow, we're going to drop the original output column. Instead, we replace it with the new output generated by our teacher model. Next, we add an LLM text block. This block generates new data using the model and the prompt template. First, we name the output of this block, in this case, reasoning trace. Since we don't want to keep this intermediate column in the final dataset, we mark it to drop from the final dataset. We then select the model that will generate the data and define the prompt that will be sent to the model. The prompt supports Jinja templates, which allows us to reference columns from the seed dataset. Finally, we add an expression block to format the final output. Expression blocks allow us to reference columns from the dataset and format them using Jinja templates. We name the output output with reasoning and define how the reasoning trace and the final answer should be combined. In this case, we place the reasoning trace between special tokens and append the final answer afterward. At this point, our pipeline is complete. Before running the full job, we can click validate to ensure the recipe is configured correctly. We're now ready to run the full data generation job. First, we give the run a human-readable name so we can easily identify it later. Then, we specify how many synthetic records we'd like to generate. For this example, we'll generate 1,000 records, though you can still get by with fewer records. We can also configure batch generation, which makes the run more resilient in case something goes wrong. Once everything is configured, we start the generation job. This process may take a little time depending on the model and the dataset size. When the run completes, you can inspect the generated records to make sure the output looks correct before moving on to the training stage. To train a model, we return to the Studio tab. First, we choose our base model. We select the Alma 2 Instruct model as the starting point for fine-tuning. Next, we select a fine-tuning method, in this case QLoRA, which is quite memory efficient. Now, we select the dataset. Here, we choose the local dataset we just generated, which contains 1,000 synthetic examples. If we click view dataset, we can inspect the structure. Studio automatically detects which columns represent the user instruction and the assistant response and uses those fields to configure the training format. If the mapping is incorrect or Studio is unable to figure out the proper mapping, you can click the AI Assist button to have it intelligently figure out that mapping for you. Next, we configure the training hyperparameters. We'll start with a learning rate of 1e-4 and adjust the LoRA parameters slightly, setting alpha to 32. For the optimizer settings, we'll keep things simple with a batch size of 10 and 100 training steps. With a dataset of 1,000 samples, this effectively trains the model for one epoch. With everything configured, we can click start training. Studio will load the base model and begin the fine-tuning process. Once training starts, we can monitor the progress directly in the interface. Studio provides several useful metrics including the training loss, gradient norm, and the learning rate schedule. These plots provide a quick sanity check that the training process is behaving as expected. In this case, we can see that the loss is gradually decreasing, which indicates that the model is learning from the dataset. On my hardware, this training run takes about 1 minute to complete. We're now ready to test this model by heading over to the chat tab. Here, we select the model we just trained, which appears under the fine-tuned section. The model may take a moment to load. Here, I ask the model a question and you can see how it begins responding. Instead of jumping straight to the final answer, the model first thinks through the problem and explains its reasoning. This indicates that the fine-tuning process worked as expected and the model has learned some new behavior from the synthetic dataset that we generated earlier. Finally, let's see how we can export the model we just trained. Head to the export tab, then select the checkpoint you'd like to export. Studio supports multiple different export formats. Here, we select a quantized format and use the recommended quantization setting. Once we start exporting the model, Studio begins the conversion process via llama.cpp. We can load our exported model in the chat interface and use it just like any other model. And that's it. Check out Onslaught Studio to try this all yourself. To learn more about NeMo Tron or NeMo Data Designer, check out the links in the description.
