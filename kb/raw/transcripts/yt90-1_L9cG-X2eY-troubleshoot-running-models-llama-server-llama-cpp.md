---
source_url: https://www.youtube.com/watch?v=1_L9cG-X2eY
video_id: 1_L9cG-X2eY
uploader: "Wes Higbee"
title: "Troubleshoot Running Models llama-server (llama.cpp)"
captured_date: 2026-08-26
status: raw_capture
provenance: youtube_video_transcript
research_run: youtube-local-llm-2026-08
---

# Troubleshoot Running Models llama-server (llama.cpp)

Today, I want to take a bit of time here and talk briefly about Llama server, so a part of Llama CPP, how you can host models with that. And specifically, if you're using it, how can you do a little bit of troubleshooting? How can you look just underneath the covers to figure out what's going on here? And so, the first thing I want to cover, of course, is starting things up here. So, up on top, I've got an example of some parameters here to start up a Qwen model here. And I'm going to start that and run that on port 8012. So, these are the args I'm passing here to the Llama server executable. Once I run that, then the very first thing you can do, take that port, hop over to a browser and hit that port. And if everything went well, you should actually see a web UI because by default, this is turned on here. This is a little chat interface. And if I split screen here, you can see if I put in a test message up above, you can see some logs coming out of Llama server. And then I've got my little chat thread down below here, which if I make this bigger, you'll actually be able to see it. So, you can interact with whatever model you've loaded without needing any other toolings. So, you don't have to use curl. You can just use this out of the box to do some troubleshooting. Of course, if you don't want this chat interface, which might be a good idea depending on where you're hosting this at here, kill off Llama server and then add a dash dash no dash and web UI. Now, when you start it up that way, give that a second. Right now, you can see I no longer have the chat interface. Instead, I've got basically an API page giving me a file not found here. And if I go to v1/models here, there you go. You can see there's the information about the models. That's a OpenAI models endpoint compatible endpoint here where you can list your models. And so, now I can see the model that I'm actually running here. All right. I'm going to cancel that cuz I'll bring that back here. I think that's helpful actually to have for testing, just have that little web chat interface. Another option, of course, you have here from the command line, you could use something like curl. Like at localhost port 8012 and then {slash} v1/models. I could do the exact same thing here inside of the terminal. I could pipe that through to jq. Let me make this a bit smaller here. And then I've got a nice interface to look through the exact same information that I have over in the browser. So, if you prefer the command line, that's another route to go. And then, when it comes to that first level of troubleshooting up on top here, so over here in the browser, when I submit a request, I get some log output here, but I don't have a lot of details. There's only probably 15 lines of log output here. And nothing in here really tells me what did I submit for a prompt, what did I get for a response. I don't have any of that, and that's because by default, the level of verbosity necessary is turned off essentially. So, you're not going to see that verbose output here. If, however, you just kill that off with Ctrl C, you can add the dash dash verbose parameter. When you do that, then come back over the browser. By the way, if you want to reset your chat thread here, Ctrl Shift and O works to start a new thread. So, I'll just ask a simple question here. What is the color of the sky? Okay, got my response here. Oh, yeah, a couple things you can see here. You have some speeds, too. So, it gives you a loose benchmark of how fast your model is performing, like 188 tokens per second here. All right. And then if I come back over to the command line with that verbose level here, I've got quite a bit of output. If you look for the word request, and this will be at the end of the log. So, go find the most recent one of these here. You'll see there's like a JSON object embedded in the logs here. So, you can copy that. And if you split the screen, then you can paste that to jq. And then you get a nice way to look at that exact same information here. And if you spend a lot of time looking at these logs here, I like to actually just copy the entire line and not try to copy from the curly brace to the ending curly brace. So, I just copy the entire line. It's easier for me. And then I've got my terminal set up with some helpers here. And I'll show you where to go to find these if you want to copy these. But essentially, I set this up here so I have an abbreviation expands here. It does the paste. And it does that to jq, but it also comes in here and strips out everything up until the very first curly brace. So, it just drops everything off the front of the line here. So, I don't have to do that and manually I go, "Okay, this part right here." Every single time I want to copy something, just copy the line. Much easier way to do that here. All right. So, I can pipe that through. You can see I get jq output here. Oh, and if you're not aware why I need to strip all that off, because with that prefix on the line there with the logging prefix here, like log server R and request here, of course, that's not going to be valid JSON. So, jq is just going to barf on you if you try to do that. Hence, I've got this PBSSE set up. It expands into this command here to just strip that off for me. Okay. And so, in this case here, what I copied was the request. So, this is specifically what went into the model. So, you can see here are messages that went in from that chat UI. I've got the user role here. And of course, I submitted this question, which is what is the color of the sky. And then you can see some of the other parameters as well. So, if you're not certain, if you thought you're setting a parameter, like maybe the temperature or top P, top K, well, this is where you can go to definitively see what did Llama server actually use for that request. Did it actually use the value that you submitted, or did it use something else? And so, it's a good way to troubleshoot if some of your settings, especially some of the default settings, are being applied the way you think they are. Especially helpful when you have some parameter names that tend to vary between different backends, like Ollama versus Llama server versus maybe OpenAI's API. I think a frequent one there is actually your max tokens parameter. If I recall correctly, that one tends to differ between different platforms. Okay. So, that's the request. You should also have an entry in here for the response. Come up a little bit here and you'll probably you should be able to find that right here. Looks like that's what we got here, at least part of it. Since this is a streaming response, I think it looks a little bit different than if it were a non-streaming response. In fact, I might just paste this in over here so I've got more space. You can see here are the choices and finish reason set to stop. And here is delta. So, in the case of a streaming response, you're going to have multiple server-sent events. Each one has pretty much one token or maybe a couple of tokens. And so, in this case, the very last one of those, the last SSE here, has a bunch of information attached to it. So, that's why we've got this delta that's empty because there's no content being sent to you on this last one. Instead, it's just basically the information to wrap up. For example, the model stopped. And so, here is the finish reason. It was a finish reason of actually just stopping. It was done. And that'd be different from maybe hitting a limit on the number of tokens you're generating. You can see things like the model again. You can also confirm some of the settings when you have verbose on, I should say. By the way, if you don't have that verbose flag set, you're not going to have this underscore underscore verbose. And so, then you're going to lose out on all this extra information, which can basically help you validate a lot of the same things you saw on the request side. You can see here's the temperature, here's top P, top K. So, you can confirm what was actually used for that entire request. And so, that's underneath of verbose. And then also underneath of verbose, if you scroll down here, you should get to a section where you can see the prompt. And this should look a bit different here. Here is the question I asked, which is what is the color of the sky. In fact, what I'm going to do here, I am going to I'm going to use jq again to parse through this and dot underscore underscore verbose. That's that verbose object you get with the verbose flag. And then dot And if I do prompt here, I can actually see what went into the model. And I can see more than just what I typed. So, it's not just the question I had here, which is what is the color of the sky. This actually includes the entire raw prompt. And if I grab the raw content value here, you can see that split across multiple lines. So, this is what the model sees. This is using the template that comes with a given model, which is Qwen 2.5 coder in this case. And it takes your messages that you pass in from the request. If you remember, we had that request object here. So, on the request here, you can see are the messages. Let me copy this one again Sometimes it's nice to actually save that out to a file. Then you can just cat out. There's the request side. So, in this case, messages go in with a role and a content section. That's the question I asked. But what actually goes in the model, that's not what the model sees. The model instead sees this format right here. Let me zoom in a little bit. So, the model sees an I am start here. We've got the system message. So, I didn't provide this. This is just provided by default. Probably coming out of the chat UI, actually, or maybe it's a default for this particular model if it's not set. And so, that is the first message you don't see. That's your system message. You could override and control that as well. And then the next thing here would be another message for the user. So, this is my question, which is what is the color of the sky. You can see the end of that message. And then the very last part here is what's known as the prefill, which prompts the model then to go ahead and respond with an assistant response. So, in this case, the template prefills the start of another message here from the assistant, just because that's exactly what you'd expect from the model. It could actually generate that as well, but a lot of the times, you'll see the a given Jinja template will insert the prefill to kind of bias how exactly the model's going to respond, especially if it has a lot of flexibility in how it responds. You can use this then to constrain For example, you could force it into a tool call if you start the format of a message that includes essentially an indicator of a tool call, then the model's going to most likely respond with a valid tool call if that's what you want to force it to do. In this case, we're just talking about forcing it to start a new message. And so, this actually is the number one thing I wanted to show you in this video was just that you can go from the messages that you pass over an API here, this JSON messages object, and you can actually see what goes into the model then with the raw prompt. And that's incredibly valuable because sometimes the template is wrong or it has a bug or it just doesn't behave as you think it should behave or maybe it's not behaving how it is documented to behave. And so, looking at the actual prompt that goes into the model can tell you if there's a problem. So, the messages you send in versus the raw prompt, sometimes you can find problems, you can fix the template or just alter the template, and then again, once you run it again, then you can check to make sure the alterations are actually applying. There's really no other way to do this than to look and see as far as the template is concerned, what is the model actually seeing? Because whatever the model sees is going to dictate then how the model responds. And so, the quality of the response, well, being able to see the raw prompt, that's a big deal. And this applies to more than just one type of model. Let me show you another one here that should underscore why this is incredibly useful, actually. So, I'm going to kill that off. The next thing I'm going to do here, instead of just running llama server manually, I am going to come over and edit a service file that has a definition for my GPT-OSS service that I run here. And I'm only going to do this because I just want to turn on the verbose level that I normally don't have on because the verbose level by default is going to add some overheads. You don't want to always run with that on. While you're learning and troubleshooting, no problem to have that on, but once you figure things out, turn that off because it's going to impact your tokens per second, it's going to impact the performance of your server if you've got to dump all that extra logging information. Remember, it was only like 15 lines of really thin output without this, but once you turn it on, it was just hundreds and hundreds of lines, thousands, maybe tens of thousands, depending on what you're passing in and getting out. All right, so when I save that, then I am going to copy that over to my server, and then I'm going to set that up to use that here. So, normally I run three user services, two of them are models. I'm running two separate instances of llama server. I've got one with Qwen 2.5 Coder, and then the other one I've got GPT-OSS. So, I run those two models all the time, and so then these service definitions are good way, if you're going to run these things regularly, you should have service definitions then so that you don't have to like find the right command in your command history every time you restart your server. Instead, you've got the service definition, which has all the parameters that you need here. You can see here are the parameters that I use as far as GPT-OSS is concerned. And then I've also got the same thing here for Qwen 2.5 Coder. I even have variants of it like the 7B and the 14B sizes. And so, if I want to look at the parameters for one of these, I can look at that right there, and I've got those values saved away in a repo. I version those, and then whenever I change them, I just push them out to my server. Then my server, so normally, once I log in, I've got user services set up with these. So, these are systemd user services. So, the second I log in to my server, these services are brought up. I don't have to go manually start those or restart those or do anything with them. Essentially, once I boot my machine, I then just log in over SSH, and that's it. The services will be brought up, and then I can use those on any of my other machines on the network because they're just up and running. And then, of course, when I'm done for the day, I like to turn the thing off because it's got a lot of fans in it. So, I just essentially go into SSH, I shut down my server, it brings everything offline. I guess I could also log out of my user account or accomplish the same thing as far as bringing the services off, but I typically shut the machine down when I'm not using it. And so then, when I come back later on, then I want to boot it up again and just SSH in, and all my services are running again. Of course, you could use system services, but I kind of like the idea of being able to bring my server up and then not yet run all my models because I don't always want them running when I boot up the server. And so, I like the idea of just SSHing in when I'm ready to start them up. All right, so in this case, you can see here I've got GPT-OSS running and Qwen Coder 14B. So, if you're doing that, then you got to go to a little bit different spot to find the logs. You use journalctl. In this case, I've got a user unit, so I've got the user flag there. And then, I'm going to follow the logs, which means I'll basically tail them and see new logs that come out. And so then, I can provide GPT-OSS, the name of my service here, and I'll see the exact same information I'd have if I had started this up manually at the command line with the llama server command. In fact, everything is basically the same thing here. If I were to just come in here and cat out that service file, you already saw it a moment ago, but you can see right here, the bulk of this service in this case is just the exact same command that I would run if I were using this from the command line directly. All right, so if I come up here, the reason I really want to show this one is this is a different port, by the way. This is running on 8013. I want to show this one because you're going to see that things look a lot different when it comes to that raw prompt. So, ask the question again, what is the color of the sky? Kind of output here. It's responding with a lot more. And once again, I can see my tokens per second, 257. Incredibly fast model, by the way. So, if I were to come over here, and if I want to look at the request and response again, just like before, so right down here was the request. So, if I copy that, I can see what I sent to the service. Same thing here, just strip off the front and show me. Yep, so there are the messages, the role and the content, exact same thing I was sending to the Qwen model in that case. And that's why it's important to have that raw prompt because obviously, what goes into the model is going to vary. So then, if I come up here, you can see a much bigger log entry here. And this will be the last one, by the way, and this is for when I'm using streaming. I split the screen here and get myself a little more room over here. So, llama server turned that into this right here. So, here's that verbose object again. Again, you have to have the verbose flag to get this. There's the prompt. So, if I want to strip that out here, dot __verbose.prompt, pull that out in the raw value, and there you go. You can see what actually goes into the model. This time looks a lot different. So, this is a harmony format with GPT-OSS versus the IM_START and END that you saw with Qwen, which is actually rather ubiquitous up until recently when a lot of models started to adopt more complex formats for messages. But so, we have the harmony format here. Just like before though, we have a system message, and I didn't set this, so this is just provided by default. This comes actually out of the templates. The Jinja template provides this one, the system message. It has things like the knowledge cutoff date, that's hardcoded in the template. It also gives it the current date, that's just by default in that template. And then, it's got the reasoning level. You can adjust this if you want low, medium, high. So, you can actually adjust that with a parameter to the API. And so then, you could use raw prompt to verify that GPT-OSS is actually seeing the level you think it's seeing and if you got the parameter correct or not. All right, so then the rest of the system message here ends off with just a note about valid channels that the harmony format uses. There's the ending tags, so just like IM_END, but it's in this case the harmony format. I start a new message, the user message, of course. I ask my question. There's the end tag, and then of course, just on the end here, just like we had with Qwen, we've got the prefill to start the next message, the assistant message, and so we've got the start in the assistant there to do that. So, that's how you get to the raw prompt, probably the most important thing to see if you're troubleshooting because you might have a couple of layers of tooling involved. You might have a chat client that needs to talk to llama server that then talks to the model, and you really need to make sure that you can actually see what the model is seeing. Otherwise, you're just guessing as to whether or not the model is getting the parameters that you want. Like if you set the reasoning level, if you can't see the raw prompt, you have no other way to verify that the model is actually doing what you think it's doing. And so then, you're just shooting in the dark. At best, you're going to be lucky if things just work out. You're going to bang your head against the wall for a long time when things aren't working. All right, and so I think that's it for right now. I think that's the bulk of what I wanted to cover was how to get to the logs here. You can also see some of the response here in the log. So, if you have a long response and you want to monitor it, you can see some of that here. If you have a request that is not streaming and is taking a while, you can watch in the logs here to see some of what it's generating as it goes if you don't have a way to turn on streaming. So, there's a lot of good information here you can get to in the logs for llama server. Make sure you have access to this, whether it's via running the command directly or via just getting to the logs for your service. And spend some time with this, get familiar with it, get comfortable with it. Go steal my little helper here. I'll show you where that's at here. Here's my repo out on GitHub. So, just go to this dotfiles repo here. Inside of this repo, I use fish shell. Fish shell has these expanding abbreviations where I can type PBSSE. The second I hit space here, boom, that expands into whatever I've defined, which you see the definition up here is this PB paste, and then I've got a reusable variable because I use this in a couple different contexts here. So, got a variant with a C that does PBSSEC here does a compact format instead. So, if I provide the compact one of these, then I basically I'm just coloring the output without expanding it across multiple lines. So, here's that file. I'll put it in the description of the video as well here. You'll find inside of here, you can copy and paste this here. If you don't use fish shell, no problem. If you look through my other repos, I've got some expanding abbreviations for both bash and Z shell as well. The Z shell and bash ones are all out inside of this dotfiles repo of mine as well. So, take a look at that, uh, and then go grab this set here. You can copy and paste these right into your own configuration and use these then. All sorts of different cases where you might want to extract things like, oh, I forgot I actually have helpers here that specifically grab the verbose prompt, so I don't actually have to type that out, the __verbose every time. Instead, in that case, if I come over and grab one of those here, PBSSE, and then I have with fish shell, and the nice thing is you can actually tab complete these. So, I just start verbose and content, or actually, I wanted PB I can do ver and prompt, verbose prompt here, and there you go. You can see that strips it all out for me. I don't have to do any of this up here with the dot __verbose.prompt. In fact, you even saw another one there. Love with fish, by the way, you can undo. So, there's, uh, prompt_harmony, which is another one I have set up, which expands to a bigger command. And the nice thing is here, this actually gives me coloring of the raw harmony prompt. Work with this so much, it's kind of nice to have some color sometimes to see blue here for the system message. And I've got purple here for the user message.
