---
source_url: https://www.youtube.com/watch?v=S0qHmLjy6gs
video_id: S0qHmLjy6gs
uploader: "Joe Maddalone"
title: "Run MLX LLMs 50% Faster on a Mac with DSpark (Speculative Decoding)"
captured_date: 2026-08-26
status: raw_capture
provenance: youtube_video_transcript
research_run: youtube-local-llm-2026-08
---

# Run MLX LLMs 50% Faster on a Mac with DSpark (Speculative Decoding)

So if you aren't aware, there is a new chapter in the speculative decoding story.  If you don't know what speculative decoding is, our most recent iteration before DSpark  was MTP multi-token prediction.  And without getting too nerdy, all models work with a pre-fill and a decode.  So the pre-fill is your input and the decode is guess what comes next.  That's your, you know, that's how LLMs work.  They're just trying to predict the next thing.  Speculative decoding was the first advance.  I think it came from, came with the Gemma model.  And that was, let's use a draft model, like a little model and have it do the decode.  So you send your input to the draft model and then it sends its guess to the target model,  the model you're, you know, really trying to use.  And then it validates that as a good or a bad guess.  Then we get MTP, which is multi-token prediction, which is like, hey, instead of doing  one at a time, let's do a bunch at a time.  And, and it does it, it still does it in an auto-regressive manner, which means it just  tries to guess the next one after the next one, after the next one.  And once you get to a bad one, that entire chain of tokens is bad.  And then it does the same process.  And the model, you know, uh, uh, validates it in a block.  And this leads to this thing called suffix decay.  So suffix decay is in that chain of tokens.  Uh, as soon as you get one that falls below a certain threshold, that's the suffix, that  part of it.  So like, you know, the first one's like, I'm 98% confident on this one.  And the third one's 30% confident.  And if that's below the threshold, the rest of it is garbage.  Uh, so there's some accuracy issues with, uh, MTP.  Um, it's fast, but accuracy can, uh, fall apart.  So D spark does more or less the same thing, but it does it in this semi auto-regressive  generation.  So it's not doing it completely sequentially.  It's trying to do it in blocks.  And then it does this, uh, the second part of the framework is, I don't see it on this  document, but it's, um, oh, it's sort of their hardware aware confidence scheduler, which  trims up the draft tokens based on available resources.  So if you're under a heavy server load, it's going to throw away a lot more garbage, a lot  quicker.  Uh, what I'm actually really excited about is this project called mlx-dspark, which brings  both D spark and D flash, uh, to native, uh, MLX on, you know, Apple Silicon.  There's a bunch of stuff here.  This is a really cool repository.  A lot of good information and statistics.  Uh, it's just Python.  So you, you know, pip install or pip X or UV or whatever you want to do.  So I've got two terminals here.  I've already installed, uh, mlx-dspark.  I'm going to run one command up here and one command down here.  Now they are identical.  And what I'm running up here is generate this model Quinn three, uh, 4 billion, 8 bit.  This prompt gives me a writing prompt just because I use that often.  It seems to generate a lot of, uh, thinking.  Uh, I've got max new token set to 128,000 because for some reason the default is 220 and  that's going to cut off a response way too early.  And then the important difference between these two commands is here.  I have mode baseline and down here I have mode dspark.  So I'm going to run this guy up here.  Now I've already downloaded this model.  Uh, and, uh, so it's skipping that part.  But if this is your first time running that model, it is going to download the model.  Now, an important thing about the models, this actually only works with a handful of models  right now.  Uh, I imagine tomorrow it'll probably run on more models.  Uh, so keep an eye on that repository for, for updates.  I think the thing I'm more excited about is, uh, I think this is going to come to mlx_lm.  And, uh, for me personally, more importantly, I think it's going to come to oMLX in time.  So this was 29.05 seconds and we got 23.2 tokens per second.  Now I'm going to run the D spark version of it.  So we can see it's doing a little bit more and it is acknowledging that we are in the D spark  speculative mode.  Visually, this seems to be running a bit faster.  So, okay.  So this took 18 seconds versus 29 seconds and we got 37.4 tokens versus 23.2 tokens.  That seems pretty significant.  Uh, now what I'm going to do is I'm going to pause this recording and go find another way  to measure this because I'm not one to really just believe in a library's own benchmarks.  So what we're going to do is we're going to run mlx-dspark as a server, which is an option  here.  I've got the same model I'm enabling, or I'm sorry, I'm disabling the prefix cache.  Um, they, when you run this in the command line, for some reason, it's max draft instead  of max new tokens.  I think that's the same, but since, uh, I'm just going to run the same test twice in two  different modes, I guess the results should be, uh, proper.  I've got no thinking on, and I'll explain that in a second.  And then mode of baseline.  Now I'm using this really cool library I found called open AI, LLM benchmark.  I will, uh, leave a link to that in the description.  Uh, but what this does, you can actually tell to do a bunch of requests and all sorts of  concurrency.  Um, but you just pointed at an open AI, uh, URL or compatible API URL, uh, put in the model  name, the number of requests and the concurrency, and it's going to spit out a number for you.  So the reason I disabled thinking is because when thinking is enabled and the API returns  that this acts as though, that's the response and it messes everything else up.  Uh, so we're going to run this on baseline.  Whoops.  I'm going to run the server first.  And now we're going to run this against the baseline and we get 21.27 tokens per second.  All right, now we're going to clear this out.  We're going to run this guy in D spark mode, clear this up and we'll run the exact same command.  Uh, and we'll see what we get.  Whoops.  I didn't start the server again.  All right.  Server is running and we get 37.69 tokens per second versus 21.27.  So I'll be the last one to pretend that that is a super scientific or well-coordinated,  uh, set of tests.  Uh, but it seems really legit.  I've run it through a bunch of different tests.  I've run it a whole bunch of times and the results are pretty consistent.  Now this is with a bunch of smaller models.  So keep that in mind.  Uh, but the results we got with the CLI directly were 1.28 times faster.  And the result we got with the, uh, open AI LLM benchmark tool that I was using was 1.77  times faster.  And if you average those out, it's 1.5 times faster.  And, uh, yeah, I'm really, really looking forward to using this with larger models.  I'm Joe Maddalone.  I make short, practical videos that respect your intelligence and your time.  If that's your style, you'll like this channel.  We'll see you next time.
